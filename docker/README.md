# Docker

Use this directory for the ROCm/vLLM container definition and setup notes.

Start from AMD's official ROCm PyTorch or vLLM image, then install this package with:

```bash
pip install -e .[dev]
```
